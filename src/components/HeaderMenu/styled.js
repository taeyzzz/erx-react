import styled from 'styled-components'

export default styled.div`
  margin: 0 20px;
  &.selected{
    color: #0044ad;
    font-weight: bold;
  }
  &:hover{
    color: #0044ad;
    cursor: pointer;
  }
`
